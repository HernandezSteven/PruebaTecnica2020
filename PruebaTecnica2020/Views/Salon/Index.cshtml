@model IEnumerable<PruebaTecnica2020.Models.Salon>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreSalon)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreSalon)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idSalon }) |
            @Html.ActionLink("Details", "Details", new { id=item.idSalon }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idSalon })
        </td>
    </tr>
}

</table>
